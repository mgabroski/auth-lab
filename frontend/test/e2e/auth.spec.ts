/**
 * frontend/test/e2e/auth.spec.ts
 *
 * Legacy auth E2E entrypoint retained only as a pointer for older tooling and
 * reviewer muscle memory.
 *
 * The real auth smoke coverage now lives in focused specs under:
 *   frontend/test/e2e/auth/
 *
 * Run with:
 *   yarn workspace frontend test:e2e:auth
 */
