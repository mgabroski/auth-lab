# frontend/.env.example
#
# Copy to frontend/.env.local for local host-run development.
# Frontend does not need OAuth client secrets — never add them here.

INTERNAL_API_URL=http://localhost:3001
NEXT_PUBLIC_ENV=development