# infra/.env.example
#
# WHY:
# - Documents the environment variables expected for local infra.
# - We keep it as an example file so we never commit real secrets.
#
# HOW TO USE:
# - Copy to infra/.env if you want to customize defaults:
#     cp infra/.env.example infra/.env
#
# NOTE:
# - We can wire docker-compose.yml to use this later via env_file.
# - For now, docker-compose.yml uses safe defaults directly.

POSTGRES_DB=auth_lab
POSTGRES_USER=auth_lab
POSTGRES_PASSWORD=auth_lab

REDIS_PORT=6379
POSTGRES_PORT=5432
